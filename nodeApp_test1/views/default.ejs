
<!DOCTYPE html>
<html lang="en">


  <head><% include partials/page/head.ejs %></head>

  <body class="<%= className %>" >
		  	<% include partials/page/header.ejs %>

				  	<div id="container">
				  		<div class="col-lg-6">

				  	<h1><%= pagetitle %></h1>

				    <h1><%= title %></h1>
				    
							<% if (typeof(users) !== 'undefined') {%>
								<ul>
								    <% for(var i = 0; i < users.length; i++) { %>
								    <li><%= users[i] %></li>
								<% } %>
								</ul>
							<% } %>


							<p>Here we define our application and set it to show on port 8080. We also have to set EJS as the view engine for our Express application using

							app.set('view engine', 'ejs');. Notice how we send a view to the user by using res.render(). It is important to note that res.render() will look in a views folder for the view. So we only have to define pages/index since the full path is views/pages/index.</p>
						</div>
					</div> <!--content ends-->



			<% include partials/page/footer.ejs %>
			<% include partials/page/jsdefaults.ejs %>

</body>
</html>






